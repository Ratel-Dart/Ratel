# Dart / Pub
.dart_tool/
.packages
build/
coverage/
doc/api/

# Library packages do not commit the lockfile
pubspec.lock

# Generated code (build_runner), produced by `ratel dev` / `ratel build`
*.ratel.dart
*.g.dart
*.reflectable.dart

# Environment & secrets
.env
*.env
!.env.example

# IDE / editor
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# SDD specs (local-only, never committed)
specs/
