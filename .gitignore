# Dart / Pub
.dart_tool/
build/
coverage/
doc/api/

# One lockfile for the whole workspace, and library packages do not commit it
pubspec.lock

# Generated by `ratel dev` / `ratel build`
*.ratel.dart

# Environment & secrets
.env
*.env
!.env.example

# IDE / editor
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# SDD specs (local-only, never committed)
specs/
